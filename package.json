{
  "name": "todo-app-with-json-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json --port 5000",
    "start": "concurrently \"npm run dev\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "json-server": "^0.17.4",
    "vite": "^7.2.2"
  },
  "dependencies": {
    "@majidh1/jalalidatepicker": "^0.9.12",
    "@tailwindcss/vite": "^4.1.17",
    "tailwindcss": "^4.1.17"
  }
}
